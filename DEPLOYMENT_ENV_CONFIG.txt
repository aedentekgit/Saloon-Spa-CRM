# --- RENDER BACKEND ENVIRONMENT VARIABLES ---
# Copy these values into the "Environment" tab of your Render Web Service.

# CORE CONFIGURATION
NODE_ENV=production
FRONTEND_URL=https://your-hostinger-domain.com
JWT_SECRET=your_very_long_random_secure_string_here

# DATABASE (MONGODB ATLAS)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/Spa_CRM

# CLOUDINARY (OPTIONAL - FOR IMAGE UPLOADS)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# EMAIL CONFIGURATION (OPTIONAL - FOR OTP/NOTIFICATIONS)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
FROM_NAME=Zen Saloon & Spa
FROM_EMAIL=noreply@yourdomain.com

# --- FRONTEND LOCAL CONFIGURATION (.env) ---
# Use this value in your LOCAL /frontend/.env file BEFORE running 'npm run build'
VITE_API_URL=https://saloon-spa-crm.onrender.com/api
